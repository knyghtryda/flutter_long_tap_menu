enum ClickType {
  click,
  rightClick,
  longPress,
  doubleClick,
  none,
}

enum MenuAlignment {
  topLeft,
  topCenter,
  topRight,
  centerLeft,
  center,
  centerRight,
  bottomLeft,
  bottomCenter,
  bottomRight
}

enum MenuPosition { inside, outside }
